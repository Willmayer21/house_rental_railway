
<div class="video-box">
    <video id="bgvid" class="video-home" playsinline autoplay muted loop poster="https://unsplash.com/photos/KxCJXXGsv9I">
      <source src="https://res.cloudinary.com/dinxoqq5s/video/upload/v1738134487/cap_ma_film_kckdvx.mp4" type="video/webm">
      <source src="https://res.cloudinary.com/dinxoqq5s/video/upload/v1738134487/cap_ma_film_kckdvx.mp4" type="video/mp4">
    </video>
    <button id="playPauseBtn" class="video-control-btn">▶</button>
    <button id="muteBtn" class="mute-btn">🔊</button>
</div>


<script>
document.addEventListener('DOMContentLoaded', function () {
  const video = document.getElementById('bgvid');
  const playPauseBtn = document.getElementById('playPauseBtn');
  const muteBtn = document.getElementById('muteBtn');
  const videoBox = document.querySelector('.video-box');

  // Ensure the video starts playing on load
  video.play().then(() => {
    videoBox.classList.add('playing'); // Hide play button
  }).catch(error => {
    console.log('Autoplay blocked:', error);
  });

  // Play/pause button functionality
  playPauseBtn.addEventListener('click', function () {
    if (video.paused) {
      video.play();
      videoBox.classList.add('playing');
      videoBox.classList.remove('paused'); // Hide play button
    }
  });

  // Click on video to toggle play/pause
  video.addEventListener('click', function () {
    if (video.paused) {
      video.play();
      videoBox.classList.add('playing');
      videoBox.classList.remove('paused'); // Hide play button
    } else {
      video.pause();
      videoBox.classList.add('paused'); // Show play button
    }
  });

  // Show play button when video is paused
  video.addEventListener('pause', function () {
    videoBox.classList.add('paused');
  });

  // Hide play button when video starts playing
  video.addEventListener('play', function () {
    videoBox.classList.remove('paused');
  });

  // Mute/unmute functionality
  muteBtn.addEventListener('click', function () {
    video.muted = !video.muted;
    muteBtn.innerHTML = video.muted ? '🔇' : '🔊'; // Change icon
  });
});



</script>


<style>


/* Video container */
.video-box {
  position: relative;
  width: 100%;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Video */
video {
  object-fit: cover;
  height: 100vh;
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  cursor: pointer; /* Make video clickable */
}

/* Play button styling */
#playPauseBtn {
  position: absolute;
  background-color: rgba(0, 0, 0, 0.7);
  color: white;
  border: none;
  border-radius: 50%;
  width: 60px;
  height: 60px;
  font-size: 24px;
  cursor: pointer;
  z-index: 1000;
  display: flex;
  justify-content: center;
  align-items: center;
  transition: opacity 0.3s ease;
  padding: 0; /* Ensures no extra spacing */
  line-height: 0; /* Prevents misalignment */
}

/* Hide play button on page load (video playing) */
.video-box.playing #playPauseBtn,
.video-box #playPauseBtn {
  opacity: 0;
  pointer-events: none; /* Prevent interaction */
}

/* Show play button only when video is paused */
.video-box.paused #playPauseBtn {
  opacity: 1;
  pointer-events: all;
}

/* Play button hover effect */
#playPauseBtn:hover {
  background-color: rgba(0, 0, 0, 0.9);
}

/* Mute button styling (kept on video) */
.mute-btn {
  position: absolute;
  bottom: 20px;
  right: 20px;
  background-color: rgba(0, 0, 0, 0.7);
  color: white;
  border: none;
  border-radius: 50%;
  width: 50px;
  height: 50px;
  font-size: 20px;
  cursor: pointer;
  z-index: 1001; /* Ensure it stays above the video */
  display: flex;
  justify-content: center;
  align-items: center;
  transition: background 0.3s ease;
}

/* Mute button hover effect */
.mute-btn:hover {
  background-color: rgba(0, 0, 0, 0.9);
}


</style>
